server:
  port: 8087

spring:
  application:
    name: client

  datasource:
    url: jdbc:postgresql://localhost:5433/db_client
    username: postgres
    password: '123456'

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"